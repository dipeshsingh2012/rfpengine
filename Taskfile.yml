version: '3'

tasks:
  test:
    desc: Run all backend tests using pytest
    cmds:
      - cd backend && pytest tests
    silent: false

  install:
    desc: Install dependencies
    cmds:
      - pip install -r backend/requirements.txt
