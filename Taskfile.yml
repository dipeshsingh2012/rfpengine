version: '3'

tasks:
  test:
    desc: Run all backend tests
    cmds:
      - export PYTHONPATH=$PYTHONPATH:$(pwd)/backend && pytest backend/tests
    silent: false

  install:
    desc: Install dependencies
    cmds:
      - pip install -r backend/requirements.txt
